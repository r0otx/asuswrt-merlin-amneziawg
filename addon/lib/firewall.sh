#!/bin/sh
# addon/lib/firewall.sh — stub. Real logic: Module 3 (Tunnel Lifecycle + PBR).

firewall_setup()    { log_warn "firewall_setup stubbed in Module 1"; return 0; }
firewall_teardown() { log_warn "firewall_teardown stubbed in Module 1"; return 0; }
